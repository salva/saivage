# F01 - Markdown Table Rendering - Analysis Review r2

## Findings

1. Section 7 still misattributes the current `package-lock.json` `markdown-it` entry and keeps a stale `typedoc` citation. The revised analysis cites [saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md](saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md#L340-L356) as though [saivage/package.json](saivage/package.json#L66) were `typedoc: "^0.28.3"`, but current [saivage/package.json](saivage/package.json#L63) is `typedoc: "^0.28.19"` and [saivage/package.json](saivage/package.json#L66) is `typescript`. More importantly, `npm ls markdown-it --all` shows only `typedoc@0.28.19 -> markdown-it@14.1.1`; the current VitePress lock block at [saivage/package-lock.json](saivage/package-lock.json#L12219-L12260) does not declare `markdown-it`, and [saivage/node_modules/vitepress/package.json](saivage/node_modules/vitepress/package.json#L120-L125) is inside VitePress's own devDependency metadata, not the consumer dependency path recorded in this repo's lockfile. The claims that npm merely dedupes VitePress under typedoc, and that removing typedoc would still leave VitePress dragging `markdown-it` into `package-lock.json`, are not supported by the current files.

2. Section 7 still has stale lockfile line citations for Mermaid's child dependencies. The parent attribution itself is correct: `mermaid@11.15.0` is the parent for `marked@16.4.2` and `dompurify@3.4.5`. But the revised analysis points `marked` at [saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md](saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md#L364-L369) and `dompurify` at [saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md](saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md#L374-L378) with the wrong dependency lines. Current [saivage/package-lock.json](saivage/package-lock.json#L8380) declares `dompurify`, and current [saivage/package-lock.json](saivage/package-lock.json#L8384) declares `marked`.

3. Open question 2 is rewritten, but it introduces a new factual error about `marked` safety configuration. The question says a parser with raw HTML disabled could use `marked` with `mangle:false, headerIds:false` plus no `<script>` / `<style>` emission at [saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md](saivage/SPEC/2026-05/markdown-table-rendering/F01-markdown-tables/01-analysis-r2.md#L529-L538). The installed `marked@16.4.2` option surface in [saivage/node_modules/marked/lib/marked.d.ts](saivage/node_modules/marked/lib/marked.d.ts#L443-L490) has no `mangle` or `headerIds` options, and [saivage/node_modules/marked/README.md](saivage/node_modules/marked/README.md#L53-L56) explicitly says Marked does not sanitize output HTML and recommends a sanitizer on the output. Those options therefore do not describe an HTML-disabled configuration that preserves the current XSS posture.

## Verified

- Section 6's four web test files all exist under [saivage/web/src](saivage/web/src), and the markdown-renderer coverage conclusion is correct: no web `*.test.ts` references `renderMarkdown`, `markdown.ts`, [saivage/web/src/components/FormattedContent.vue](saivage/web/src/components/FormattedContent.vue), or [saivage/web/src/components/ChatWindow.vue](saivage/web/src/components/ChatWindow.vue).
- The refreshed non-lockfile line numbers specifically requested for `telegramify-markdown`, `typedoc-plugin-markdown`, `vitepress`, `happy-dom`, `vitest`, [saivage/web/package.json](saivage/web/package.json#L12-L16) runtime deps, and [saivage/web/src/components/FormattedContent.vue](saivage/web/src/components/FormattedContent.vue#L83-L84) `pre-wrap` match the current files.
- Section 3's task-list and nested-list descriptions match [saivage/web/src/utils/markdown.ts](saivage/web/src/utils/markdown.ts#L19-L26): task-list rows become ordinary bullets with `[ ]` / `[x]` preserved in text, while indented nested bullets remain raw.
- Section 4 no longer overstates table metadata as unrecoverable; the revised wording correctly scopes the limitation to the current regex-only renderer architecture.
- Section 8 is now code-derived, and Section 8b clearly carries the project policy rather than mixing it into the factual constraints.
- Section 7 no longer contains bundle-size estimates; it now gives a measurement plan.

VERDICT: CHANGES_REQUESTED

1. Correct the Section 7 `markdown-it` provenance, `typedoc` package citation, and `typedoc` version.
2. Correct the Section 7 Mermaid child dependency line citations for `dompurify` and `marked`.
3. Rewrite open question 2 so it does not present invalid `marked` options as an HTML-disabled or XSS-preserving configuration.