# G49 — APPROVED

**Chosen proposal**: Per [02-design-r4.md](02-design-r4.md) — shared Zod envelope schema module at src/channels/ws-schema.ts with one alias (`@channels/ws-schema`) wired in [vitest.config.ts](../../../../vitest.config.ts), [web/vite.config.ts](../../../../web/vite.config.ts), and [web/tsconfig.json](../../../../web/tsconfig.json) (server keeps relative `./ws-schema.js` import). Fail-loud validation at both boundaries: server `1003` close on inbound schema violation; browser `ws.close()` (no custom code; browsers send 1005) with a structured `console.warn`, a bounded `errors` ring entry, and a best-effort upstream `{type:"error", reason, raw?}` envelope before close. Every `WsOutbound` variant `.strict()`; the server sender always emits `JSON.stringify(WsOutboundSchema.parse(event))`. SPA `send` wraps the emitter with `WsInboundSchema.parse(msg)` so `as any`/debug callers can no longer leak drift past the browser boundary. Removed the unbounded `events.value` array (replaced with an emitter API). Deleted duplicate `WsEvent` interfaces and the two `ChatChannel & { sendEvent?: ... }` escape hatches in one atomic PR (no shim).

**Approved by**: GPT-5.5 (copilot) reviewer at round 4 — see [04-review-r4.md](04-review-r4.md). All four rounds of blockers resolved: r1 framed the shared-schema approach; r2 corrected the browser close code and added strict outbound validation + SPA send validation + Vitest config widening + happy-dom; r3 added the resolver alias wiring across vitest/vite/tsconfig and moved the smoke project path to workspace-local `tmp/`; r4 replaced the broken `node -e 'import(...)'` acceptance command with `node --import tsx -e ...` (tsx@^4.21.0 verified in devDependencies).

**Implementation pointer**: [03-plan-r4.md](03-plan-r4.md). New project-wide principles satisfied: schema-driven envelope (no regex on inbound data prose; bare strings still routed to chat agent as raw user prose, by design); no new hardcoded knobs; no fragile agent-tool-call heuristics.

**Daemon impact**: Restart `saivage` (10.0.3.111), `saivage-v3` (10.0.3.112), `diedrico` (10.0.3.113) — operator-gated.
