# G21 - Review of round 3

## Findings

No blocking findings. Round 3 satisfies the three required changes from [04-review-r2.md](04-review-r2.md#L25-L29) and the remaining plan is aligned with the architecture-first, no-shim direction for this issue.

## Verification

The arbitrary-key regression is now corrected. R2 asked the writer to stop expecting the raw non-slashed fallback string in `buildChain()` output ([04-review-r2.md](04-review-r2.md#L27)). R3's planned test now configures an arbitrary `providerConfigs` key and asserts only that the router does not expand it into `not-a-real-provider/claude-sonnet-4.6` ([03-plan-r3.md](03-plan-r3.md#L176-L191)). That matches the live chain path: provider-only expansion currently happens only at the `isProviderName` check ([src/providers/router.ts](../../../../src/providers/router.ts#L553-L557)); after descriptor-only membership, the unknown fallback is passed to `appendCandidatesForModelSpec` ([src/providers/router.ts](../../../../src/providers/router.ts#L561-L569)) and then filtered through provider-independent candidate expansion over registered providers ([src/providers/router.ts](../../../../src/providers/router.ts#L577-L582), [src/providers/router.ts](../../../../src/providers/router.ts#L616-L630)). R3's prose now states this same non-containment contract and drops the incorrect raw-string expectation ([01-analysis-r3.md](01-analysis-r3.md#L39-L40), [02-design-r3.md](02-design-r3.md#L130-L145), [03-plan-r3.md](03-plan-r3.md#L197-L200)).

The positive built-in provider-only expansion coverage is now present. R2 required an explicit no-`modelEquivalents` case proving descriptor names still expand through the failover guard ([04-review-r2.md](04-review-r2.md#L28)). R3 adds exactly that shape: `failover: { "github-copilot": ["openai-codex"] }`, no model equivalents, and an assertion that `openai-codex/claude-sonnet-4.6` appears in the chain ([03-plan-r3.md](03-plan-r3.md#L145-L169)). This targets the live code path that turns a provider-only fallback into a provider/model spec ([src/providers/router.ts](../../../../src/providers/router.ts#L545-L557)). It also fills the real gap in the existing tests: the nearby failover tests use full provider/model specs or explicit equivalents ([src/providers/router.test.ts](../../../../src/providers/router.test.ts#L70-L122), while the legacy provider-only test is a suppression case because `modelEquivalents.has(modelSpec)` skips expansion ([src/providers/router.test.ts](../../../../src/providers/router.test.ts#L267-L283), [src/providers/router.ts](../../../../src/providers/router.ts#L553-L556)).

The test-impact and validation prose is now accurate. R3 explicitly labels the existing `does not carry provider-only failover onto models with explicit equivalents` test as suppression coverage, not positive expansion coverage ([01-analysis-r3.md](01-analysis-r3.md#L61-L63), [02-design-r3.md](02-design-r3.md#L140-L146), [03-plan-r3.md](03-plan-r3.md#L219-L221)). That is consistent with the live test's expectation that `openai-codex/claude-sonnet-4.6` is absent when an explicit equivalent exists ([src/providers/router.test.ts](../../../../src/providers/router.test.ts#L267-L283), and with the suppression guard in the router ([src/providers/router.ts](../../../../src/providers/router.ts#L553-L556)).

The architecture remains appropriately direct. The plan replaces the four independent name authorities in the current router, namely the boot list, registration switch, factory switch, and `isProviderName` literal/provider-config fallback ([src/providers/router.ts](../../../../src/providers/router.ts#L105-L118), [src/providers/router.ts](../../../../src/providers/router.ts#L731-L754), [src/providers/router.ts](../../../../src/providers/router.ts#L766-L815), [src/providers/router.ts](../../../../src/providers/router.ts#L871-L881), with a single descriptor table and derived `ProviderName` ([03-plan-r3.md](03-plan-r3.md#L40-L87)), descriptor-driven initialization ([03-plan-r3.md](03-plan-r3.md#L100-L107)), deletion of `shouldRegisterProvider` ([03-plan-r3.md](03-plan-r3.md#L111-L113)), descriptor-backed creation ([03-plan-r3.md](03-plan-r3.md#L115-L126)), and descriptor-only `isProviderName` ([03-plan-r3.md](03-plan-r3.md#L129-L142)). This removes the compatibility fallback rather than preserving it, keeps the change two-file-scoped as stated in the plan ([03-plan-r3.md](03-plan-r3.md#L1-L3), [03-plan-r3.md](03-plan-r3.md#L203-L209)), and avoids introducing a plugin layer or migration shim.

## Residual risk

The remaining risk is implementation fidelity: the descriptor closures must mirror the current provider constructors and registration predicates exactly, and validation must catch any accidental widening of `ProviderName`. R3 calls out that risk in the typecheck step and focused router/provider test runs ([03-plan-r3.md](03-plan-r3.md#L211-L227), and the done criteria directly check that the duplicate sites are gone and the descriptor-only guard is in place ([03-plan-r3.md](03-plan-r3.md#L243-L252)).

VERDICT: APPROVED