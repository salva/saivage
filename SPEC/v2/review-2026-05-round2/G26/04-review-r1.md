# G26 - Review (round 1, GPT-5.5)

## Findings

1. The validation gates conflict with the required schema-rejection test and with the task order. Task 3's grep gate asks for zero `model_overrides` hits across source before Task 4 removes the existing legacy resolver test, but the current test still contains the legacy fixture and `source: "legacy"` assertion at [src/routing/resolver.test.ts](../../../../src/routing/resolver.test.ts#L5-L28). Task 4b then asks the writer to add a schema test whose fixture contains `model_overrides` at [SPEC/v2/review-2026-05-round2/G26/03-plan-r1.md](03-plan-r1.md#L226-L245), while Task 6 requires zero `model_overrides` hits across source and guide docs at [SPEC/v2/review-2026-05-round2/G26/03-plan-r1.md](03-plan-r1.md#L296-L304). That final gate will fail even if the implementation is correct. Required change: either scope the zero-match grep to production source and docs while explicitly allowing the one schema-rejection test hit, or construct the rejection fixture without a literal `model_overrides` key. Also move or narrow the Task 3 grep so it does not run before the resolver test cleanup, and keep a separate gate that no test asserts the `legacy` source string.

2. The production-deadness and daemon-impact framing is internally inconsistent. The live code proves that `model_overrides` is obsolete by policy but not dead as an accepted production input: [src/types.ts](../../../../src/types.ts#L12-L16) accepts it, [src/store/project.ts](../../../../src/store/project.ts#L125-L133) seeds an empty stub, and [src/routing/resolver.ts](../../../../src/routing/resolver.ts#L254-L267) still consumes non-empty overrides and maps them to `legacy`. The analysis then says the legacy arm is "proven to never fire" at [SPEC/v2/review-2026-05-round2/G26/01-analysis-r1.md](01-analysis-r1.md#L57-L60), and the design claims no daemon impact for projects with an empty field at [SPEC/v2/review-2026-05-round2/G26/02-design-r1.md](02-design-r1.md#L270-L276). Under the proposed `.strict()` behavior at [SPEC/v2/review-2026-05-round2/G26/02-design-r1.md](02-design-r1.md#L34-L40), any top-level `model_overrides`, including `{}`, fails config load. Required change: revise the analysis/design to say the field is a live legacy input that must be removed and rejected, not a branch that cannot fire. Update the daemon note so existing empty stubs are called out as strict-load failures that need operator-gated cleanup before restart, without adding a migration shim.

## What Holds

The selected architecture is otherwise the right one: remove the schema field, add top-level strictness, stop seeding the stub, delete `resolveLegacyModels`, narrow `ResolvedModelRoute.source` to `"routing" | "runtime-default"`, and clean the guide docs. The current line references for the primary edit sites match the live tree: [src/types.ts](../../../../src/types.ts#L12-L29), [src/routing/resolver.ts](../../../../src/routing/resolver.ts#L54-L75), [src/routing/resolver.ts](../../../../src/routing/resolver.ts#L106-L127), [src/routing/resolver.ts](../../../../src/routing/resolver.ts#L204-L269), and [src/store/project.ts](../../../../src/store/project.ts#L125-L133).

Keeping `ResolvedModelRoute.source` while narrowing away only the `legacy` literal is also correct. G25 still relies on the field as a routing-derived diagnostic for allow-list-only rules at [SPEC/v2/review-2026-05-round2/G25/02-design-r1.md](../G25/02-design-r1.md#L226-L230), and the current resolver test records that behavior at [src/routing/resolver.test.ts](../../../../src/routing/resolver.test.ts#L121-L136). G26 should not delete the whole field.

The G24 sequencing is acceptable once the above changes are made: if G24 lands first, `ProjectRoutingConfigLike` disappears per [SPEC/v2/review-2026-05-round2/G24/APPROVED.md](../G24/APPROVED.md#L3-L7), and G26's schema deletion naturally removes the legacy input from the derived resolver input. If G25 is revised before approval, G26 still needs a final rebase check around [src/routing/resolver.ts](../../../../src/routing/resolver.ts#L204-L219), but the intended helper deletion remains compatible.

## Required Changes

1. Fix the grep gates so the required schema-rejection test and resolver-test cleanup can both pass.
2. Correct the analysis/design language around production liveness and strict-schema daemon impact for existing empty stubs.

VERDICT: CHANGES_REQUESTED