# G13 — APPROVED

**Chosen proposal**: Proposal A (per [02-design-r3.md](02-design-r3.md) which carries forward [02-design-r2.md](02-design-r2.md) unchanged) — extract `LocalChatCommand`, `LOCAL_CHAT_COMMANDS`, `LocalChatCommandName`, and `renderLocalChatCommandsTable` from [src/agents/conventions.ts](../../../../src/agents/conventions.ts#L60-L93) into a new pure-data chat-owned leaf module [src/chat/localCommandRegistry.ts](../../../../src/chat/localCommandRegistry.ts). The territory rules (`ConventionRule`, `checkConvention`, `getConvention`) stay in `src/agents/conventions.ts`. The unused `checkConvention` import at [src/agents/base.ts](../../../../src/agents/base.ts#L35) is removed. Test file [src/agents/chat-commands.test.ts](../../../../src/agents/chat-commands.test.ts) is `git mv`'d to [src/chat/localCommandRegistry.test.ts](../../../../src/chat/localCommandRegistry.test.ts). Proposal B (inline into `src/chat/localCommands.ts`) is explicitly rejected — it would drag `runtime/notes`, `events/bus`, and `server/bootstrap` transitive surface into the prompt loader and keep catalogue/dispatcher tests tangled.

**Approved by**: GPT-5.5 (copilot) reviewer at round 3 — see [04-review-r3.md](04-review-r3.md). All required changes addressed: corrected the analysis (the territory checker is NOT wired into production runtime; only an unused import existed at base.ts L35), added `npm run lint` to validation before build (per [package.json](../../../../package.json#L20) and [eslint.config.js](../../../../eslint.config.js#L11-L14)), removed the proposed extra file-level docblock, and replaced the broken `from.*agents/conventions` grep with both same-directory and cross-directory patterns to validate the actual surviving consumers.

**Implementation pointer**: [03-plan-r3.md](03-plan-r3.md). Post-split conventions consumers are exactly [src/agents/roster.ts](../../../../src/agents/roster.ts#L11-L31), [src/agents/agents.test.ts](../../../../src/agents/agents.test.ts#L10-L86), and [src/agents/roster.test.ts](../../../../src/agents/roster.test.ts#L24-L130). Behavioural invariants (`/help` output, `renderLocalChatCommandsTable()` string, `LocalChatCommandName` literal union via `satisfies`, `checkConvention` warnings) stay byte-identical.

**Daemon impact**: None required. No saivage-v3 restart required.
