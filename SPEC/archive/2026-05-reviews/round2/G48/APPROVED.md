# G48 — APPROVED

**Chosen proposal**: Proposal B (per [02-design-r3.md](02-design-r3.md)) — extract a `withRuntime(projectPath, fn)` helper shared by `start` and `inspect` CLI actions, encoding the bootstrap/teardown invariant in one place. Replaces the current asymmetry where `start` uses `try/finally` correctly but `inspect` leaks MCP children, supervisor, and the runtime lockfile when the action throws. Actions (`startAction`, `inspectAction`) and the helper extracted to a new side-effect-free [src/server/cli-actions.ts](src/server/cli-actions.ts) so test imports don't trigger commander's `program.parse()`. `serve` deliberately untouched (its existing try/finally remains; documented). Partial-bootstrap-failure teardown scoped OUT (filed as [G51-partial-bootstrap-teardown.md](../G51-partial-bootstrap-teardown.md)). Proposal A (local try/finally in `inspect`) rejected — duplicates the invariant.

**Approved by**: GPT-5.5 (copilot) reviewer at round 3 — see [04-review-r3.md](04-review-r3.md). All three rounds of blockers resolved: r1 framed the helper; r2 added cli-actions.ts extraction + per-kind histogram leak detection + /proc/self/fd delta + AST-based invariants test using the TypeScript compiler API + Fatal→Error normalization + shutdown-only failure test; r3 filed the G51 stub and normalized the T8 prefix contract (`Error:` for action/bootstrap failures, `Shutdown error:` for shutdown-only failures; exit code follows the action outcome, not teardown).

**Implementation pointer**: [03-plan-r3.md](03-plan-r3.md). Tests: T1-T6 unit tests against the helper (happy/throw/bootstrap-throw/exitCode/shutdown-failure/exactly-once); T7 e2e leak test using `process.getActiveResourcesInfo()` per-kind histogram + `/proc/self/fd` length delta; T8 shutdown-only failure test; AST-based invariant test pinning that `bootstrap()` and `.shutdown()` co-occur only inside `withRuntime` or `serve`.

**Daemon impact**: Restart `saivage` (10.0.3.111), `saivage-v3` (10.0.3.112), `diedrico` (10.0.3.113) — operator-gated. New principles compliance documented (n/a — pure resource-leak fix).
