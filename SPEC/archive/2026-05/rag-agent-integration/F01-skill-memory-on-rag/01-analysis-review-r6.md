# Review - 01-analysis-r6

1. Add `kind: "openai"` to both protected dataset `provider` objects in section 2. The analysis declares `PROTECTED_DATASETS` as `RuntimeRagDatasetConfig[]`, and the runtime dataset type is `Omit<DatasetConfig, "projectId">` ([saivage/src/rag/manager.ts](saivage/src/rag/manager.ts#L34-L39)); `DatasetConfig.provider` is an `EmbeddingProviderRef`, whose shape requires `kind: "openai"`, `model`, and `dim` ([saivage/src/rag/types.ts](saivage/src/rag/types.ts#L12-L16), [saivage/src/rag/types.ts](saivage/src/rag/types.ts#L39-L44)). The current section 2 snippet only specifies `provider: { model: "text-embedding-3-small", dim: 1024 }` for both datasets ([saivage/SPEC/2026-05/rag-agent-integration/F01-skill-memory-on-rag/01-analysis-r6.md](saivage/SPEC/2026-05/rag-agent-integration/F01-skill-memory-on-rag/01-analysis-r6.md#L280-L289)), so the protected dataset declaration remains source-inaccurate as written.

The seven requested correction checks are otherwise addressed: section 0.1 distinguishes the seed index shapes from the rebuild `{ entries }` shape; section 0.8 explicitly calls out `body_path` removal and the `update_memory` scope preflight; section 4.3 uses `filter = { in: { path: pathsForIds } }`, matching `QueryFilter`'s `in` branch ([saivage/src/rag/types.ts](saivage/src/rag/types.ts#L114-L120)); section 7.2 relies on `store.getFileState()` instead of KNN enumeration ([saivage/src/rag/store/sqlite-vec.ts](saivage/src/rag/store/sqlite-vec.ts#L432-L439)); section 0.2 matches skill search scoring as `+3` for name or trigger, `+2` for description, and `+1` for body ([saivage/src/knowledge/loader.ts](saivage/src/knowledge/loader.ts#L117-L135)); and sections 2 and 6.1.c include `store: { kind: "sqlite-vec" }` while using the `RuntimeRagDatasetConfig` typing. The existing source confirms why the planned `body_path` and `update_memory` changes are behavioral deltas ([saivage/src/knowledge/types.ts](saivage/src/knowledge/types.ts#L105-L115), [saivage/src/mcp/knowledgeMemory.ts](saivage/src/mcp/knowledgeMemory.ts#L216-L228)).

VERDICT: CHANGES_REQUESTED